{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window permissions (core + dialog open).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}

